/** The portal's `labelFieldStyle` translated to a shared Tailwind class. */
export const boFieldClass =
    'rounded-none border-0 border-b border-foreground bg-transparent px-0 shadow-none focus-visible:border-primary focus-visible:ring-0';
